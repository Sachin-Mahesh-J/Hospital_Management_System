generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// No models are defined in Milestone 1. The physical-schema approval gate in
// docs/database/logical-data-model.md must pass before models or migrations are added.
